Usuario NO autenticado:
├─ Intenta acceder a /home → Redirige a /cuenta/login ✅
├─ Intenta acceder a /materias → Redirige a /cuenta/login ✅
├─ Intenta acceder a /progreso → Redirige a /cuenta/login ✅
├─ Intenta acceder a /classRoom → Redirige a /cuenta/login ✅
├─ Accede a / → Muestra landing page ✅
├─ Accede a /cuenta/login → Muestra formulario ✅
└─ Accede a /examen → Permite acceso ✅

Usuario autenticado:
├─ Accede a / → Redirige a /home ✅
├─ Accede a /cuenta/login → Redirige a /home ✅
├─ Accede a /cuenta → Muestra página de cuenta ✅
├─ Accede a /home → Muestra home ✅
├─ Accede a /materias → Muestra materias ✅
├─ Accede a /progreso → Muestra progreso ✅
└─ Accede a /classRoom → Muestra classroom ✅